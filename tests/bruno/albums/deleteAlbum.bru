meta {
  name: deleteAlbum
  type: http
  seq: 2
}

delete {
  url: {{BASE_URL}}/albums/{{albums.createdId}}
  body: none
  auth: inherit
}

script:post-response {
  // if (res.getStatus() === 204) {
  //   bru.setVar('albums.createdId', undefined)
  // }
}

settings {
  encodeUrl: true
  timeout: 0
}
